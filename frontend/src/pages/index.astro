---
import HeaderNotLogged from '../components/HeaderNotLogged.astro';
import Layout from '../layouts/Layout.astro';
import Footer from '../components/Footer.astro';
import Hero from '../components/Hero';
import FeaturesComponent from '../components/FeaturesComponent';
---
<Layout title="ForgeHabits">
    <HeaderNotLogged/>
    <main class= "max-w-8xl px-4 m-auto min-h-screen" >
        <div class="bg-gray-700 h-full flex flex-col justify-center items-center m-10 rounded-2xl p-6 space-y-6">
            <Hero></Hero>
        </div>
        <div class="bg-gray-700 h-full flex flex-col justify-center items-center m-10 rounded-2xl p-6 space-y-6">
            <FeaturesComponent></FeaturesComponent>
        </div>
        <div class="bg-gray-700 h-full flex flex-col justify-center items-center m-10 rounded-2xl p-6 space-y-6">
            <div class="text-center mt-10">
                <h2 class="text-3xl text-white font-bold text-center ">¿Por qué unirse a <span class="text-orange-200">ForgeHabits</span>?</h2>

                <p class="text-gray-200 max-w-lg mx-auto mt-4">
                    Al unirte a ForgeHabits, obtendrás acceso a herramientas y recursos que te ayudarán a construir y mantener hábitos saludables y productivos.
                </p>
                <ul class="list-disc list-inside text-gray-200 mt-4 space-y-2">
                    <li>Seguimiento diario de hábitos</li>
                    <li>Gráficos y análisis de progreso</li>
                    <li>Comunidad de apoyo</li>
                    <li>Recordatorios personalizados</li>
                </ul>
            </div>
            <div class="text-center mt-10">
                <h2 class="text-2xl font-bold text-gray-900">Testimonios</h2>
                <div class="flex flex-wrap justify-center mt-4 space-x-4">
                    <div class="bg-white p-4 rounded-2xl shadow-lg max-w-xs">
                        <p class="text-gray-700">"ForgeHabits ha cambiado mi vida. Ahora soy mucho más productivo y organizado." - Juan P.</p>
                    </div>
                    <div class="bg-white p-4 rounded-2xl shadow-lg max-w-xs">
                        <p class="text-gray-700">"Gracias a ForgeHabits he logrado mantener hábitos saludables que antes no podía." - María S.</p>
                    </div>
                </div>
            </div>
        </div>

       
    </main>
    <Footer/>
</Layout>

<script type="module">
    async function checkAuth() {
        const token = sessionStorage.getItem('jwtToken');
        console.log('Token: ' + token);
        
        if (token) {
            try {
                const response = await fetch('http://localhost:3000/api/auth/status', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': 'Bearer ' + token,
                    }
                });

                const data = await response.json();

                if (response.ok && data.isLoggedIn) {
                    console.log('User is logged in.');
                    window.location.href = '/dashboard';
                }
            } catch (error) {
                console.error('Authentication check failed:', error);
            }
        } else {
            console.log('No token found -> User persists as not logged in.');
        }
    }

    document.addEventListener('DOMContentLoaded', function() {
        checkAuth();
    });
</script>